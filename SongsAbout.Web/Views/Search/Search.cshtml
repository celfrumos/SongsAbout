@using SongsAbout.Web.Models
    @{
        ViewBag.Title = "Search";
    }

    <h2>Search</h2>

    @foreach (var item in ((SearchResult)ViewBag.Results).Artists)
    {
        @Html.DisplayFor(a => item)
    }