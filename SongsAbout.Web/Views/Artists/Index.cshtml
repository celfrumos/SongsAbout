@using SongsAbout.Web.Models;
@model List<Artist>

@{
    ViewBag.Title = "Artists";
}

<h2>Artists</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@for (int i = 0; i < Model.Count; i++)
{
    Artist item = Model[i];
    <div class="col-md-6">
        <a href="/Artists/Details?id=@item.ArtistId"><img src="@(item.ProfilePic?.Url)" width="200" , height="200" alt="@(item.ProfilePic?.AltText?? "img-"+item.Name)" /></a>
        <br />
        <span class="artist-name" id="tile-@item.Name">@item.Name</span>
    </div>
}
<div>
    Viewing @ViewBag.Count out of @ViewBag.TotalCount

</div>

@* TODO: limit page sizes *@

